package com.company;
/*
import com.sun.org.apache.xml.internal.security.exceptions.Base64DecodingException;
import com.sun.tools.doclint.DocLint;
import com.sun.tools.internal.ws.wscompile.BadCommandLineException;
import com.sun.tools.javac.code.Types;
import com.sun.tools.javac.jvm.ClassReader;
import org.omg.CORBA.*;
import org.omg.CORBA.TypeCodePackage.*;
import com.*;
import javax.*;
import java.*;
import javax.crypto.AEADBadTagException;
import javax.crypto.BadPaddingException;
import javax.naming.directory.AttributeInUseException;
import javax.security.auth.login.AccountException;
import javax.security.sasl.AuthenticationException;
import java.awt.*;
import java.lang.annotation.AnnotationFormatError;
import java.lang.annotation.AnnotationTypeMismatchException;
import java.nio.BufferOverflowException;
import java.nio.channels.AcceptPendingException;
import java.nio.channels.AlreadyBoundException;
import java.nio.channels.AlreadyConnectedException;
import java.nio.channels.AsynchronousCloseException;
import java.nio.file.AccessDeniedException;
import java.nio.file.AtomicMoveNotSupportedException;
import java.rmi.activation.ActivateFailedException;
import java.security.AccessControlException;
import java.security.acl.AclNotFoundException;
*/

import java.util.ArrayList;
import java.util.Arrays;

public class Main {

       /*public static void main(String[] args) {throws ArithmeticException, ArrayIndexOutOfBoundsException, ArrayStoreException, AssertionError,
            AnnotationFormatError, AnnotationTypeMismatchException, AcceptPendingException, AbstractMethodError, AlreadyBoundException, AsynchronousCloseException, AccessDeniedException,
            AccessControlException, AtomicMoveNotSupportedException, AlreadyConnectedException, AclNotFoundException, ActivateFailedException, AWTError, AccountException,
            AuthenticationException, AttributeInUseException, ACTIVITY_COMPLETED, ACTIVITY_REQUIRED, Types.AdaptFailure, ClassReader.BadClassFile, BAD_CONTEXT, BAD_INV_ORDER, BAD_OPERATION, BAD_PARAM,
            BAD_QOS, BAD_TYPECODE, DocLint.BadArgs, BadKind, org.omg.CORBA.TypeCodePackage.Bounds, BadCommandLineException, BootstrapMethodError, BufferOverflowException, BadPaddingException, Base64DecodingException{*/

        /*SavingsAccount myAccount = new SavingsAccount(200, 5);
        myAccount.deposit(132.14);
        myAccount.addInterest();
        System.out.println("Final Balance is: " + myAccount.getBalance());
        */
        /*
        int [] nums = {3,0,4,2,1};
        int len = nums.length;
        for (int i = 0; i < len; i++){
            for (int count = 1; count <= len; count++){
                nums[i] = nums[nums[i]];
                for (int a:nums) {
                    System.out.print(a + "\t");
                }
                System.out.print("\n");
            }
        }
        */

        /*
        boolean a = false;
        boolean b = false;

        if (a&&b || a&&!b || !a&&b || !a&&!b){

        }
        */
        //Print(9999999);
    //int [][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][][] test;
    public static void main(String[] args) {

    }
}
